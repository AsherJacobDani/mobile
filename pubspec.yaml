name: mobile
description: InstaAI Mobile Application
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  dio: ^5.9.0
  flutter_secure_storage: ^9.2.4
  http_parser: ^4.1.2
  image_picker: ^1.2.0
  intl: ^0.20.2
  photo_manager: ^3.7.1
  url_launcher: ^6.3.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true